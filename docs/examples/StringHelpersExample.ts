
export const randomString = {
  title: 'Generate random string',
  code: `
// The result here keeps changing because how code mirror dispatch their updates
result = StringHelpers.randomString(32)
  `
}